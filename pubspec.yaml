name: test1
description: A new Flutter application, Test 1

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  firebase_core: ^0.5.0
  firebase_auth: ^0.18.0+1
  cloud_firestore:
  provider: ^4.3.2+1
  mailer: ^3.0.3
  flutter_email_sender: ^4.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - IIIImages/

